<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{config.title}}</title>
  <link rel="stylesheet" href="/styles/main.css">
  {% if resolvedUrl %}
  <meta http-equiv="Refresh" content="2; url='{{ resolvedUrl }}'" />
  {% endif %}
</head>
<body>
  <a href="/" class="logo">
    {% include 'logo.svg' %}
  </a>
	{% block content %}{{ content | safe }}{% endblock %}
</body>
</html>
